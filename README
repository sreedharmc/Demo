===============================================================
Global setup:
 Set up git
  git config --global user.name "Sreedhar Mc"
  git config --global user.email sreedhar@consultant.com
      
Next steps:
  mkdir Demo
  cd Demo
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:sreedharmc/Demo.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:sreedharmc/Demo.git
  git push -u origin master
      
Importing a Subversion Repo?
  Check out the guide for step by step instructions.
      
When you're done:
  Continue
===========================================================
NEW LINES ARE ADDED...
